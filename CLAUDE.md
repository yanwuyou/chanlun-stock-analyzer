# CLAUDE.md

## 项目概述

缠论股票技术分析工具，基于缠中说禅《教你炒股票108课》完整理论体系实现。

分析管线：数据获取 → 包含处理 → 分型 → 笔 → 线段 → 中枢 → 走势类型 → 背驰(MACD) → 三类买卖点 → 防狼术 → 表里关系 → 中枢监视器 → 同级别分解 → 机械化信号链 → 底部构造 → 综合决策

## 核心文件

- `chan_analyzer.py` — 主引擎 + CLI入口（你只需要跟这个文件打交道）
- `chan_lib/data.py` — 数据获取（Baostock + 本地CSV缓存）
- `chan_lib/morphology.py` — 形态学（包含→分型→笔→线段）
- `chan_lib/dynamics.py` — 动力学（中枢→走势→背驰→买卖点）
- `chan_lib/strategy.py` — 策略层（防狼术→中阴→区间套→决策引擎）
- `gui_app.py` — tkinter 桌面GUI
- `activation.py` — 激活模块
- `watchlist_manager.py` — 自选池管理
- `keygen.py` — 激活码生成器（卖家工具）
- `缠论股析.md` — 完整技能定义（分析规则、使用命令、缠论知识）

## 常用命令

### 单只股票分析
```bash
python chan_analyzer.py <代码> <名称>
```
例：`python chan_analyzer.py 601689 拓普集团`

### 多级别联立分析（30分钟+日线+周线）
```bash
python chan_analyzer.py --full <代码> <名称>
```

### 批量扫描自选池
```bash
python chan_analyzer.py --watchlist
```

### 沪深300全市场筛选
```bash
python chan_analyzer.py --csi300 [数量]
```

### 生成激活码
```bash
python keygen.py <机器码>
```

### 打包EXE
```bash
build.bat
```

### 修改自选池
编辑 `chan_analyzer.py` 底部的 `DEFAULT_WATCHLIST` 列表，格式：`("代码", "名称")`

## 使用流程（缠论标准）

1. **防狼术过滤**：只看 MACD 日线 DIFF 在 0 轴之上的品种
2. **中枢定位**：找操作级别最后一个中枢，判断价格在中枢上/中/下
3. **等买点**：一买（中枢下方背驰）、二买（一买后回踩）、三买（中枢上方突破回抽）
4. **铁律**：买点只在下跌中形成，绝不追涨；卖点只在上涨中形成，绝不恐慌割肉

## 输出规则（强制）

**每条结论必须带理由，用大白话说清楚"是什么 + 为什么"。** 格式：

> 结论：XXX。理由：XXX。

禁止只给结论不给理由。禁止堆术语。

术语翻译表：

- "防狼术 DIFF>0" → "趋势指标在安全区"，"(-1,0)" → "短期回调接近尾声，正在筑底"
- "中枢" → "主力成本区"，"背驰" → "下跌动能衰减"，"走势类型 uptrend" → "中长期处于上升通道"
- 用生活化比喻（"像皮球拍下去了"、"像车在十字路口"），不堆砌数据，读起来像人话不是研报

## 内部参考（术语速查）

- `[防狼术]`：MACD 0轴之上=安全可操作，之下=远离不做
- `(-1,1)`：向下笔延伸中，持币等待
- `(-1,0)`：底分型构造中，准备买入（需底分型确认）
- `(1,1)`：向上笔延伸中，持股待涨
- `(1,0)`：顶分型构造中，准备卖出
- `一买`：下跌趋势背驰终点，利润最大但风险最高
- `二买`：一买后的回踩低点，安全性最高，散户首选
- `三买`：突破中枢后的回抽确认，效率最高

## 选股推荐流程

用户要求推荐股票时，必须跑实际分析，不能空口说白话：
1. 从沪深300或自选池扫描
2. 按防狼术过滤（DIFF > 0）
3. 按表里关系分类：(-1,0)优先 → (-1,1)+底背驰次之
4. 分三个梯队输出，每只附带关键指标和具体操作建议

## 环境

- Python 命令：`python`（非 `python3`）
- 数据源：Baostock（免费、无限流、无需注册、无需token）
- 依赖：`pip install baostock pandas numpy`
- GUI依赖：tkinter（Python 自带）
- 打包：PyInstaller
