# 缠论股析

> 基于缠中说禅《教你炒股票108课》完整理论体系的量化分析工具
> 4935行代码 / 82个函数 / 108课全部可算法化内容
> **当前版本：v1.1.1（2026-05-29）**

## 技能概述

对任意A股股票执行完整的缠论技术分析，覆盖**数据获取 → 包含处理 → 分型 → 笔 → 线段 → 中枢 → 走势类型 → 背驰(MACD) → 三类买卖点 → 防狼术 → 机械化信号 → 综合决策**十二条管线，最终输出明确的操作建议（买入/卖出/持股/持币/观望 + 置信度）。

**v1.1 新增：** K线缠论图可视化、HTML图文报告（浏览器阅读）、自定义分析日期区间、报告质量体系（白话解读/星级评分/6条件背驰检查/一句话总结）。

**v1.1.1 稳健性优化：** `--end` / GUI 截止日期真正参与数据获取和分析；多级别联立默认统一为 30分钟+日线+周线+月线；网络异常时优先使用本地缓存快速兜底，避免离线等待过久。

## 使用场景

- 用户要求分析一只股票的技术面
- 用户要求推荐股票（筛选+排序）
- 用户询问某只股票该不该买/卖/持有
- 用户要求批量扫描自选池或沪深300

## 项目结构

```
E:\缠论股票分析\
├── chan_analyzer.py        ← 主引擎 + CLI入口（唯⼀需要调⽤的⽂件）
├── gui_app.py              ← tkinter 桌面GUI（含日期选择器、报告区、进度反馈）
├── chan_lib/
│   ├── data.py             ← 数据获取（Baostock + 本地缓存）
│   ├── morphology.py       ← 形态学（包含→分型→笔→线段）
│   ├── dynamics.py         ← 动力学（中枢→走势→背驰→买卖点）
│   ├── strategy.py         ← 策略层（防狼术→中阴→区间套→决策引擎）
│   └── visual.py           ← v1.1新增：K线图可视化（mplfinance+matplotlib）
├── activation.py           ← 激活模块（一机一码）
├── watchlist_manager.py    ← 自选池管理
├── keygen.py               ← 激活码生成器（卖家工具）
├── build.bat               ← PyInstaller 打包脚本
├── cache/                  ← 本地数据缓存（自动过期）
├── 缠论股析.md              ← 本文件（技能定义）
└── CLAUDE.md               ← 项目规则
```

## 执行命令

### 单只股票完整分析（最常用）

```bash
cd E:\缠论股票分析 && python chan_analyzer.py <股票代码> <股票名称>
```

可选参数：
- `--start YYYYMMDD`：自定义分析起始日期（默认 20200101）
- `--end YYYYMMDD`：自定义分析截止日期（默认今天；用于历史复盘/回测）
- `--chart`：分析完成后生成 K 线缠论图（保存到 `./charts/` 目录）

例：`python chan_analyzer.py --start 20180101 --end 20240531 601689 拓普集团`

输出包含：防狼术、多级别防狼、形态学、走势类型、中枢、背驰（6条件检查）、买卖点、表里关系、中枢监视器、同级别分解、底部构造、几何能量统一、机械化信号、综合决策。

### 多级别联立分析（30分钟+日线+周线+月线）

```bash
cd E:\缠论股票分析 && python chan_analyzer.py --full <股票代码> <股票名称>
```

v1.1.1 起默认包含月线级别。支持 `--start` / `--end` 参数。分析完成后自动生成 HTML 图文报告（含 K 线缠论图），在浏览器中打开。

### 批量扫描自选池

```bash
cd E:\缠论股票分析 && python chan_analyzer.py --watchlist
```

支持 `--start` / `--end` 参数透传。

### 沪深300全市场筛选

```bash
cd E:\缠论股票分析 && python chan_analyzer.py --csi300 [数量]
```

支持 `--start` / `--end` 参数透传。

## 分析管线

```
K线数据 → 包含关系处理(第62课) → 分型识别(第62课) → 笔的划分(第62/77课)
→ 线段划分(第67/71/78课) → 中枢识别(第17/20课) → 走势类型判断(第15/17课)
→ 背驰判断-6条件(第24/37课) → 三类买卖点(第20/21/53课)
→ 防狼术(第103课) → 表里关系(第91/93课) → 中枢监视器(第92课)
→ 中阴阶段(第88-90课) → 区间套(第27/61课) → 同级别分解(第38-40课)
→ 机械化信号链(第105课) → 底部构造(第108课) → 综合决策
```

## 缠论分析必须遵守的规则

### 防狼术（第一道闸门）

- MACD日线DIFF > 0 → 可以操作
- MACD日线DIFF < 0 → 远离不做，不看不分析

**分析任何股票前，必须先过防狼术这关。DIFF在0轴之下的股票，直接给出"远离不做"结论，不需要继续分析。**

### 表里关系四种状态

| 状态 | 含义 | 操作 |
|------|------|------|
| (1,1) | 向上笔延伸中 | 持股待涨 |
| (-1,1) | 向下笔延伸中 | 持币等待 |
| (1,0) | 顶分型构造中 | 准备卖出 |
| (-1,0) | 底分型构造中 | 准备买入 |

### 买点铁律

- 买点只在下跌中形成——(-1,1)或(-1,0)状态
- 绝不追涨——(1,1)状态不宜新开仓
- (-1,0) ≠ 立即买入，必须等底分型确认（第三根K线收盘站上第一根高点）

### 卖点铁律

- 卖点只在上涨中形成——(1,1)或(1,0)状态
- 绝不恐慌割肉——(-1,1)状态不宜卖出

### 三类买卖点

| 买点 | 位置 | 特征 |
|------|------|------|
| 一买 | 中枢下方+趋势背驰终点 | 利润最大，风险最高 |
| 二买 | 一买后回踩低点 | 安全性最高，散户首选 |
| 三买 | 中枢上方+突破回抽确认 | 效率最高 |

### 背驰判断（6条件）

趋势背驰需要验证6个条件：同级别中枢、c包含三类买卖点、b级别不大于c、c创新高/低、B回拉0轴、MACD面积/黄白线不创新高/低。

### 选股结论输出规则（强制）

**每条结论必须带理由，格式：结论 + 理由。禁止只给结论不给理由。**

用最通俗的大白话来写，禁用缠论术语（不要出现"底分型"、"中枢"、"背驰"、"DIFF"、"(-1,0)"等），翻译成普通人能听懂的语言：

- "防狼术过滤 DIFF>0" → "趋势指标在安全区"
- "(-1,0) 底分型构造中" → "短期回调接近尾声，正在筑底"
- "(-1,1)" → "短期还在调整中"
- "(1,1)" → "短期在上涨中"
- "中枢" → "主力成本区" / "多空平衡区"
- "中枢上方" → "价格跑在成本区之上"
- "背驰" → "下跌动能衰减"
- "走势类型 uptrend" → "中长期处于上升通道"
- "三买" → "突破后回踩确认"
- "底部构造 complete" → "底部形态已确认"
- "等底分型确认" → "等一个确认信号——某天收盘价突破前几天的最高点"
- "止损设在XX" → "万一跌破XX就离场"

**不堆砌数据**。每个推荐最多写3-4句核心逻辑，用生活化的比喻（"像皮球拍下去了"、"像车在十字路口"）。写完后自己读一遍——如果读起来像券商研报，就重写。

### 筛选选股流程

1. 防狼术过滤：只看DIFF > 0的股票
2. 表里关系分类：(-1,0)优先 → (-1,1)+底背驰次之 → (1,1)持有不宜追
3. 中枢位置：价格在中枢上方或中枢内部偏强为佳
4. 走势类型：上涨趋势(up-trend) > 盘整(consolidation) > 下跌趋势(down-trend)
5. 底部构造：complete > in_progress > failed
6. 多信号共振确认：防狼术 + 表里关系 + 中枢位置 + 底部构造 同时发出正向信号

### 选股结论输出格式

给出选股结论时，分三个梯队：
- **第一梯队（准备买入）**：(-1,0)状态，走势类型向上或盘整，底部构造完成
- **第二梯队（等待信号）**：(-1,1)状态但有底背驰，等(-1,0)出现
- **第三梯队（持有不宜追）**：(1,1)状态，趋势完美但位置偏高

每只推荐的股票必须给出：关键指标表格 + 买入理由 + 操作建议（具体买入条件、止损位）

### 止损纪律

买入后如价格跌破操作级别中枢下沿，无条件离场。

## K线图可视化（v1.1 新增）

分析完成后自动生成专业的缠论 K 线图，叠加全部缠论元素，以 HTML 图文报告形式在浏览器中展示。

**图表元素：**
- K 线（A股习惯红涨绿跌）+ 成交量副图（单位"万"）+ MACD 副图
- 笔连线（彩色虚线，lw=1.2，分型高低点圆形标记）
- 线段连线（彩色实线，lw=3.0，端点方形标记）
- 中枢区间（半透明矩形，alpha=0.22，标注 ZG/ZD 价格）
- 三类买卖点（三角箭头：买点红色向上、卖点绿色向下）

**技术参数（固化，不可配置）：**
- 最多显示 300 根 K 线（自动裁剪，分析本身使用全量数据）
- 单根 K 线宽度 0.085 英寸，画布动态宽度 = max(18, min(44, n×0.085))
- 中枢透明度 22%，笔 zorder=2，线段 zorder=2，标记 zorder=8-10
- 时间轴连续无空白间隙（show_nontrading=False）
- X 轴标签 45° 旋转，全局中文字体适配（微软雅黑 → 黑体 → 宋体）

**图表生成流程：**
1. 分析完成后，在主线程中调用 `chart_to_base64()` 逐级别生成 base64 编码 PNG
2. 嵌入 `generate_html_report()` 生成的完整 HTML 文档
3. 自动在默认浏览器中打开

## HTML 图文报告（v1.1 新增）

日线分析和多级别联立分析均可生成。**架构要点：HTML 生成统一在主线程执行（非后台线程），日线和多级别联立共享同一 `_generate_html_report()` 入口。**

**报告内容：**
- 概览卡片：代码、名称、最新价、分析时间、数据范围
- 防狼术结论区：安全区（绿框）/ 警告区（黄框）/ 危险区（红框横幅 + 周线交叉验证）
- K 线缠论图：每级别独立一张，base64 内嵌
- 各级别分析卡片：走势类型 + 白话 / 中枢定位 + 白话 / 背驰 6 条件逐项检查 / 买卖点星级评分 / 表里关系 / 中枢监视器 / 中阴阶段 / 底部构造
- 白话总结：5 维度网格（大方向/姿态/力量/信号/位置）+ 一句话总结

**报告质量体系（v1.1 新增）：**
- 白话解读全覆盖：所有缠论术语附带通俗解释，用生活化比喻（中枢="主力成本区"，背驰="皮球拍下去弹不起来了"）
- 结论 + 理由双行结构：每条结论带"理由：XXX"
- 星级评分：买卖点标注可靠性等级 ★/★★/★★★ + 质量评价
- 背驰 6 条件逐项检查：每条件列出 ✓/✗ + 具体数值
- 一句话总结：报告末尾综合所有信号给出核心操作方向
- 层级索引导航：一~六编号段落，卡片式布局快速定位

## 自定义分析日期区间（v1.1 新增）

**CLI 方式：** `--start YYYYMMDD` 参数，如 `--start 20180101`。

**GUI 方式：** 起始日期（年/月/日独立下拉框）+ 截止日期（年/月/日下拉框 + "到今天"复选框）。
- 起始日期默认 2020-01-01，年份范围 2010 ~ 当前
- "到今天"默认勾选，自动取当天日期
- 取消勾选可手动设定截止日期（适用于历史复盘/回测）
- 截止日期早于起始日期时，自动提示错误，不发起分析

**数据源日期限制：**
- Baostock 日线数据最早约 1990 年，30 分钟数据最早约 2015 年
- 月线级别自动取 20100101 起（保证足够 K 线数）
- 周线级别自动取 20150101 起
- 起始日期设得过早不会报错，系统自动取数据源最早可用日期

## GUI 桌面应用（v1.1）

tkinter 桌面 GUI（`gui_app.py`），打包为单文件 EXE（`《缠论》股析.exe`，约 303 MB）。

**界面布局三大区域：**
- **自选池（左侧）：** 管理关注股票列表，双击快速分析，右键删除
- **分析面板（右上）：** 输入代码/名称 + 日期区间设置 + 日线分析/多级别联立按钮
- **报告区（右下）：** 文字报告 + "查看完整报告(含K线图)"按钮 + "复制报告"按钮

**交互优化：**
- 状态栏实时进度反馈："正在获取数据..." → "正在生成报告..." → "正在生成K线图表..." → "报告生成完成"
- 分析中按钮禁用，防止重复点击（`_analysis_running` 标志位）
- 报告区文字分区高亮：蓝色标题 · 绿色买入 · 红色卖出 · 橙色警告
- 统一配色：绿色按钮 #27ae60 / 浅灰背景 #f1f3f5 / 深灰文字 #2c3e50

**激活：** 一机一码，机器码绑定硬件。重装系统不影响激活，更换电脑需联系卖家。

## 数据源

- **主力数据源**：Baostock（免费、无限流、无需注册、无需token）
- **兜底**：本地CSV缓存（TTL分级管理：日线24h、周线7d、分钟线1-8h）
- 缓存过期后自动拉新，拉不到用过期缓存兜底；已有缓存时会快速试探网络，避免离线等待过久
- 大级别数据自动提前起始日期以保证足够 K 线数（月线≥12根、周线≥20根、日线/30分钟≥30根）

## 注意事项

1. 缠论分析基于技术面，不涉及基本面/消息面
2. 分析结论仅供参考，不构成投资建议
3. 所有判断基于概率，不存在100%准确的技术分析方法
4. 仓位管理和止损纪律和技术分析同样重要
5. 分析级别默认日线，多级别联立（含月线）可提高置信度
6. 用户问股票时，不要空口说白话——必须先跑分析再看结论
7. K 线图最多显示 300 根（为图表可读性），分析本身使用全量数据不受影响
8. 30 分钟数据最早约 2015 年（Baostock 限制），更早的起始日会自动取最早可用日
9. 仅支持 Windows 打包（EXE），macOS 需 Python 源码运行
10. 需要网络连接获取数据，离线仅可用已有本地缓存
